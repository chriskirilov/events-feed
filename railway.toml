[build]
builder = "nixpacks"

[deploy]
startCommand = "gunicorn run_server:app --bind 0.0.0.0:$PORT --workers 2 --timeout 300"
healthcheckPath = "/"
healthcheckTimeout = 5
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
